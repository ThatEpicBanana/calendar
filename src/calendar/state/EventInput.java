public class EventInput implements InputLayer {
    public InputLayer handleInput(char command) {
        // handles input logic for the Add layer
        return null; // return null if nothing happens //
    }
}